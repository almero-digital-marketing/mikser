'use strict'
var express = require('express');
var path = require('path');
var fs = require("fs-extra-promise");

module.exports = function (mikser) {
	var debug = mikser.debug('server-views');
	return (app) => {
	}
}